# 临时情绪系统

这是一个独立的 AstrBot 插件，目录必须直接放在 AstrBot 的 `data/plugins/astrbot_plugin_temp_emotion` 下才会被扫描加载。它不能作为 `astrbot_plugin_private_companion` 的子目录直接启用。

如与边界与情感反馈插件一起使用，边界事件会通过已注册的 AstrBot 插件实例注入临时情绪状态。

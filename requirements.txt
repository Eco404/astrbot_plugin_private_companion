# AstrBot itself supplies the astrbot package and is intentionally not listed here.
# These libraries back the plugin's network, image, and calendar integrations.
aiohttp>=3.9,<4
Pillow>=10,<13
chinese-calendar>=1.9.2
lunarcalendar>=0.0.9
# Reality Touch routes decoded TTS audio directly to a user-selected output endpoint.
numpy>=1.26,<3
sounddevice>=0.5,<1
soundfile>=0.12,<1
# Reality Touch camera capture uses OpenCV VideoCapture without GUI components.
opencv-python-headless>=4.10,<5
